# QUESTION: Detect A cycle in Undirected Graph using BFS
# Given an undirected graph with V vertices labeled from 0 to V-1. The graph is represented using
# an adjacency list where adj[i] lists all nodes connected to node i. Determine if the graph
# contains any cycles.
# Note: The graph does not contain any self-edges (edges where a vertex is connected to itself).
#
# Examples:
# Example 1:
# Input: V = 6, adj = [[1, 3], [0, 2, 4], [1, 5], [0, 4], [1, 3, 5], [2, 4]]
# Output: True
# Explanation: The graph contains a cycle: 1 -> 2 -> 5 -> 4 -> 1.
#
# Example 2:
# Input: V = 4, adj = [[1, 2], [0], [0, 3], [2]]
# Output: False
# Explanation: The graph does not contain any cycles.
#
# Constraints:
# 1 <= V, E <= 10^4 (E = number of edges)


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def detect_a_cycle_in_undirected_graph_using_bfs_brute(self) -> None:
        pass

    def detect_a_cycle_in_undirected_graph_using_bfs_better(self) -> None:
        pass

    def detect_a_cycle_in_undirected_graph_using_bfs_optimal(self) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
