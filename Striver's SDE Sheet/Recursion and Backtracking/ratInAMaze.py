# QUESTION: Rat in a Maze
# Given a grid of dimensions n x n. A rat is placed at coordinates (0, 0) and wants to reach at coordinates (n-1, n-1). Find all possible paths that rat can take to travel from (0.
#
# Examples:
# Input: n = 4 , grid = [ [1, 0, 0, 0] , [1, 1, 0, 1], [1, 1, 0, 0], [0, 1, 1, 1] ]
# Output: ["DDRDRR" , "DRDDRR"]
# Explanation: The rat has two different path to reach (3, 3).
# The first path is (0, 0) => (1, 0) => (2, 0) => (2, 1) => (3, 1) => (3, 2) => (3, 3).
# The second path is (0,0) => (1,0) => (1,1) => (2,1) => (3,1) => (3,2) => (3,3).
#
# Input: n = 2 , grid = [[1, 0] , [1, 0]]
# Output: []
# Explanation: There is no path that rat can choose to travel from (0,0) to (1,1).


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def rat_in_a_maze_brute(self) -> None:
        pass

    def rat_in_a_maze_better(self) -> None:
        pass

    def rat_in_a_maze_optimal(self) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
