# QUESTION: Inorder Traversal
# Given the root of a binary tree, return the inorder traversal of its
# nodes' values. Inorder traversal visits each node in the order:
# Left subtree → Root → Right subtree.
#
# Examples:
# Example 1:
# Input: root = [1, 4, null, 4, 2]
# Output: [4, 4, 2, 1]
# Explanation: Inorder traversal of the binary tree produces [4, 4, 2, 1].
#
# Example 2:
# Input: root = [1, null, 2, 3]
# Output: [1, 3, 2]
# Explanation: Inorder traversal produces the sequence [1, 3, 2].
#
# Constraints:
# The number of nodes in the tree is in the range [0, 100].
# -100 <= Node.val <= 100
#
# Follow up: Recursive solution is trivial. Could you do it iteratively
# (typically using an explicit stack)?


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def inorder_traversal_brute(self) -> None:
        pass

    def inorder_traversal_better(self) -> None:
        pass

    def inorder_traversal_optimal(self) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
