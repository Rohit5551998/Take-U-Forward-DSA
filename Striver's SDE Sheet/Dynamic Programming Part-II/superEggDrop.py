# QUESTION: Super Egg Drop
# You are given n identical eggs and a building with k floors numbered
# from 1 to k. There exists an unknown floor f (0 <= f <= k) such that:
#   - Any egg dropped from a floor HIGHER than f will break.
#   - Any egg dropped from floor f or BELOW will not break.
# In each move, you may take an unbroken egg and drop it from any floor
# x (1 <= x <= k). If the egg breaks, you can no longer use it. If it
# doesn't break, you may reuse it in subsequent moves. Your goal is to
# determine the value of f with CERTAINTY. Return the minimum number of
# moves required to determine f, in the worst case.
#
# Examples:
# Example 1:
# Input: n = 1, k = 2
# Output: 2
# Explanation: With 1 egg, you must try floors one-by-one bottom-up.
#
# Example 2:
# Input: n = 2, k = 6
# Output: 3
#
# Example 3:
# Input: n = 3, k = 14
# Output: 4
#
# Constraints:
# 1 <= n <= 100
# 1 <= k <= 10^4


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


def super_egg_drop_brute() -> None:
    pass


def super_egg_drop_better() -> None:
    pass


def super_egg_drop_optimal() -> None:
    pass
