# QUESTION: Roman to Integer
# Roman numerals are represented by seven different symbols with the following values:
# I = 1, V = 5, X = 10, L = 50, C = 100, D = 500, M = 1000.
# For example, 2 is written as II (two ones added together), 12 is written as XII (X + II), and
# 27 is written as XXVII (XX + V + II). Roman numerals are usually written largest to smallest
# from left to right. However, when a smaller symbol appears before a larger one, it is
# subtracted. There are six such cases:
# - I can be placed before V (5) and X (10) to make 4 and 9.
# - X can be placed before L (50) and C (100) to make 40 and 90.
# - C can be placed before D (500) and M (1000) to make 400 and 900.
# Given a roman numeral string s, convert it to an integer and return it.
#
# Examples:
# Example 1:
# Input: s = "LVIII"
# Output: 58
# Explanation: L = 50, V = 5, III = 3.
#
# Example 2:
# Input: s = "MCMXCIV"
# Output: 1994
# Explanation: M = 1000, CM = 900, XC = 90 and IV = 4.
#
# Constraints:
# - 1 <= s.length <= 15
# - s contains only the characters 'I', 'V', 'X', 'L', 'C', 'D', 'M'.
# - It is guaranteed that s is a valid roman numeral representing a number in [1, 3999].


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def roman_to_integer_brute(self) -> None:
        pass

    def roman_to_integer_better(self) -> None:
        pass

    def roman_to_integer_optimal(self) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
