# mypy: disable-error-code="empty-body"
# QUESTION: Check if Two Strings Are Anagram of Each Other (Valid Anagram)
# Given two strings s and t, return true if t is an anagram of s, and false
# otherwise. An anagram is a word or phrase formed by rearranging the letters of a
# different word or phrase, typically using all the original letters exactly once.
# Example 1:
# Input: s = "anagram", t = "nagaram"
# Output: true
# Explanation: Both strings contain the same characters with the same frequencies.
# Constraints:
# 1 <= s.length, t.length <= 5 * 10^4
# s and t consist of lowercase English letters.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def isAnagram_brute(self, s: str, t: str) -> bool:
        pass

    def isAnagram_better(self, s: str, t: str) -> bool:
        pass

    def isAnagram_optimal(self, s: str, t: str) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.isAnagram_optimal("anagram", "nagaram"))
