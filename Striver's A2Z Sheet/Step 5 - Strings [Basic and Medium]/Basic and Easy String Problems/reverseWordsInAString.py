# mypy: disable-error-code="empty-body"
# QUESTION: Reverse Words in a String
# Given an input string s, reverse the order of the words. A word is defined as a
# sequence of non-space characters. The words in s will be separated by at least
# one space. Return a string of the words in reverse order concatenated by a
# single space. Note that s may contain leading or trailing spaces or multiple
# spaces between two words. The returned string should only have a single space
# separating the words. Do not include any extra spaces.
# Example 1:
# Input: s = "the sky is blue"
# Output: "blue is sky the"
# Explanation: The words are reversed in order with single spaces between them.
# Constraints:
# 1 <= s.length <= 10^4
# s contains English letters (upper-case and lower-case), digits, and spaces ' '.
# There is at least one word in s.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def reverseWords_brute(self, s: str) -> str:
        pass

    def reverseWords_better(self, s: str) -> str:
        pass

    def reverseWords_optimal(self, s: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.reverseWords_optimal("the sky is blue"))
