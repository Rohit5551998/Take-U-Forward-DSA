# mypy: disable-error-code="empty-body"
# QUESTION: Largest Odd Number in String
# You are given a string num, representing a large integer. Return the largest-value
# odd integer (as a string) that is a non-empty substring of num, or an empty
# string "" if no odd integer exists. A substring is a contiguous sequence of
# characters within a string.
# Example 1:
# Input: num = "52"
# Output: "5"
# Explanation: The only non-empty substrings are "5", "2", and "52". "5" is the
# only odd number.
# Constraints:
# 1 <= num.length <= 10^5
# num only consists of digits and does not contain any leading zeros.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def largestOddNumber_brute(self, num: str) -> str:
        pass

    def largestOddNumber_better(self, num: str) -> str:
        pass

    def largestOddNumber_optimal(self, num: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.largestOddNumber_optimal("52"))
