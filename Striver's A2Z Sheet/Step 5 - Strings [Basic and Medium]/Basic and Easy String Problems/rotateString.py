# mypy: disable-error-code="empty-body"
# QUESTION: Check Whether One String is a Rotation of Another (Rotate String)
# Given two strings s and goal, return true if and only if s can become goal after
# some number of shifts on s. A shift on s consists of moving the leftmost
# character of s to the rightmost position. For example, if s = "abcde", then it
# will be "bcdea" after one shift.
# Example 1:
# Input: s = "abcde", goal = "cdeab"
# Output: true
# Explanation: Shifting "abcde" two times produces "cdeab".
# Constraints:
# 1 <= s.length, goal.length <= 100
# s and goal consist of lowercase English letters.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def rotateString_brute(self, s: str, goal: str) -> bool:
        pass

    def rotateString_better(self, s: str, goal: str) -> bool:
        pass

    def rotateString_optimal(self, s: str, goal: str) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.rotateString_optimal("abcde", "cdeab"))
