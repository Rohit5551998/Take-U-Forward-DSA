# mypy: disable-error-code="empty-body"
# QUESTION: Isomorphic Strings
# Given two strings s and t, determine if they are isomorphic. Two strings s and t
# are isomorphic if the characters in s can be replaced to get t. All occurrences
# of a character must be replaced with another character while preserving the
# order of characters. No two characters may map to the same character, but a
# character may map to itself.
# Example 1:
# Input: s = "egg", t = "add"
# Output: true
# Explanation: 'e' maps to 'a' and 'g' maps to 'd' consistently, one-to-one.
# Constraints:
# 1 <= s.length <= 5 * 10^4
# t.length == s.length
# s and t consist of any valid ASCII character.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def isIsomorphic_brute(self, s: str, t: str) -> bool:
        pass

    def isIsomorphic_better(self, s: str, t: str) -> bool:
        pass

    def isIsomorphic_optimal(self, s: str, t: str) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.isIsomorphic_optimal("egg", "add"))
