# mypy: disable-error-code="empty-body"
# QUESTION: Longest Palindromic Substring [Do it without DP]
# Given a string s, return the longest palindromic substring in s. A string is a
# palindrome when it reads the same backward as forward. A substring is a
# contiguous sequence of characters within the string.
# Example 1:
# Input: s = "babad"
# Output: "bab"
# Explanation: "aba" is also a valid answer of the same length.
# Constraints:
# 1 <= s.length <= 1000
# s consist of only digits and English letters.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def longestPalindrome_brute(self, s: str) -> str:
        pass

    def longestPalindrome_better(self, s: str) -> str:
        pass

    def longestPalindrome_optimal(self, s: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.longestPalindrome_optimal("babad"))
