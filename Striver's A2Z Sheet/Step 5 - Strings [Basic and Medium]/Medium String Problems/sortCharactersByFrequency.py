# mypy: disable-error-code="empty-body"
# QUESTION: Sort Characters by Frequency
# Given a string s, sort it in decreasing order based on the frequency of the
# characters. The frequency of a character is the number of times it appears in the
# string. Return the sorted string. If there are multiple answers, return any of
# them.
# Example 1:
# Input: s = "tree"
# Output: "eert"
# Explanation: 'e' appears twice while 'r' and 't' appear once. So 'e' must appear
# before both 'r' and 't'. "eetr" is also a valid answer.
# Constraints:
# 1 <= s.length <= 5 * 10^5
# s consists of uppercase and lowercase English letters and digits.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def frequencySort_brute(self, s: str) -> str:
        pass

    def frequencySort_better(self, s: str) -> str:
        pass

    def frequencySort_optimal(self, s: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.frequencySort_optimal("tree"))
