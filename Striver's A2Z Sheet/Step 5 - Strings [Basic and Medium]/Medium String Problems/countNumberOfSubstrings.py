# mypy: disable-error-code="empty-body"
# QUESTION: Count Number of Substrings (with exactly K distinct characters)
# Given a string s consisting of lowercase letters and an integer k, return the
# number of substrings of s that contain exactly k distinct characters. A substring
# is a contiguous sequence of characters within the string.
# Example 1:
# Input: s = "aba", k = 2
# Output: 3
# Explanation: The substrings with exactly 2 distinct characters are "ab", "ba",
# and "aba".
# Constraints:
# 1 <= s.length <= 10^5
# 1 <= k <= 26
# s consists only of lowercase English letters.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def countSubstrings_brute(self, s: str, k: int) -> int:
        pass

    def countSubstrings_better(self, s: str, k: int) -> int:
        pass

    def countSubstrings_optimal(self, s: str, k: int) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.countSubstrings_optimal("aba", 2))
