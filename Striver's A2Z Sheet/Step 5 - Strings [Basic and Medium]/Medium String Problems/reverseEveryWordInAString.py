# mypy: disable-error-code="empty-body"
# QUESTION: Reverse Every Word in a String
# Given a string s, reverse the order of the words in the string. A word is a
# maximal sequence of non-space characters. Words are separated by one or more
# spaces. The result must contain the words in reverse order joined by a single
# space, with no leading or trailing spaces and no repeated spaces between words.
# Example 1:
# Input: s = "  hello   world  "
# Output: "world hello"
# Explanation: Extra leading, trailing and inner spaces are removed, and the words
# are placed in reverse order.
# Constraints:
# 1 <= s.length <= 10^4
# s contains English letters (upper/lower case), digits, and spaces ' '.
# There is at least one word in s.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def reverseEveryWord_brute(self, s: str) -> str:
        pass

    def reverseEveryWord_better(self, s: str) -> str:
        pass

    def reverseEveryWord_optimal(self, s: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.reverseEveryWord_optimal("  hello   world  "))
