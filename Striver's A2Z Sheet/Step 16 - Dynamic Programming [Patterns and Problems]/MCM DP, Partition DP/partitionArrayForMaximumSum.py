# mypy: disable-error-code="empty-body"
# QUESTION: Partition Array for Maximum Sum
# Partition arr into contiguous subarrays of length at most k. Each subarray's
# values become its maximum. Return the largest possible resulting sum.
#
# Example 1:
# Input: arr = [1,15,7,9,2,5,10], k = 3
# Output: 84
#
# Constraints:
# 1 <= n <= 500
# 1 <= k <= n

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def max_sum_after_partitioning_brute(self, arr: List[int], k: int) -> int:
        pass

    def max_sum_after_partitioning_better(self, arr: List[int], k: int) -> int:
        pass

    def max_sum_after_partitioning_optimal(self, arr: List[int], k: int) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.max_sum_after_partitioning_optimal([1, 15, 7, 9, 2, 5, 10], 3)
