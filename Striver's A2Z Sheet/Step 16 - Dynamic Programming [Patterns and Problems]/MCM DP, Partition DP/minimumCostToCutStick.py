# mypy: disable-error-code="empty-body"
# QUESTION: Minimum Cost to Cut the Stick
# A stick of length n and positions cuts[]. The cost of a cut equals the
# current length of the stick being cut. Cuts can be done in any order; return
# the minimum total cost.
#
# Example 1:
# Input: n = 7, cuts = [1,3,4,5]
# Output: 16
#
# Constraints:
# 2 <= n <= 10^6
# 1 <= len(cuts) <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def min_cost_cut_stick_brute(self, n: int, cuts: List[int]) -> int:
        pass

    def min_cost_cut_stick_better(self, n: int, cuts: List[int]) -> int:
        pass

    def min_cost_cut_stick_optimal(self, n: int, cuts: List[int]) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.min_cost_cut_stick_optimal(7, [1, 3, 4, 5])
