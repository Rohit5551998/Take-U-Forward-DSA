# mypy: disable-error-code="empty-body"
# QUESTION: Count Subsets With Sum K
# Given non-negative integers and target k, count subsets whose sum equals k.
#
# Example 1:
# Input: nums = [1,2,2,3], k = 3
# Output: 3
# Explanation: [1,2],[1,2],[3].
#
# Constraints:
# 1 <= n <= 100
# 0 <= nums[i] <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def count_subsets_sum_k_brute(self, nums: List[int], k: int) -> int:
        pass

    def count_subsets_sum_k_better(self, nums: List[int], k: int) -> int:
        pass

    def count_subsets_sum_k_optimal(self, nums: List[int], k: int) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.count_subsets_sum_k_optimal([1, 2, 2, 3], 3)
