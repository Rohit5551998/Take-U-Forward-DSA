# mypy: disable-error-code="empty-body"
# QUESTION: Subset Sum Equal To K
# Given an array of non-negative integers and target k, determine whether a
# subset exists whose sum equals k.
#
# Example 1:
# Input: nums = [1,2,3,4], k = 4
# Output: True
# Explanation: subset [1,3] or [4].
#
# Constraints:
# 1 <= n <= 100
# 0 <= nums[i] <= 10^4

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def subset_sum_k_brute(self, nums: List[int], k: int) -> bool:
        pass

    def subset_sum_k_better(self, nums: List[int], k: int) -> bool:
        pass

    def subset_sum_k_optimal(self, nums: List[int], k: int) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.subset_sum_k_optimal([1, 2, 3, 4], 4)
