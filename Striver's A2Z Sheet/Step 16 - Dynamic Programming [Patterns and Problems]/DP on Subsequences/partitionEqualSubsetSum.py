# mypy: disable-error-code="empty-body"
# QUESTION: Partition Equal Subset Sum
# Return True if the array can be partitioned into two subsets with equal sum.
#
# Example 1:
# Input: nums = [1,5,11,5]
# Output: True
# Explanation: [1,5,5] and [11].
#
# Constraints:
# 1 <= n <= 200
# 1 <= nums[i] <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def can_partition_brute(self, nums: List[int]) -> bool:
        pass

    def can_partition_better(self, nums: List[int]) -> bool:
        pass

    def can_partition_optimal(self, nums: List[int]) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.can_partition_optimal([1, 5, 11, 5])
