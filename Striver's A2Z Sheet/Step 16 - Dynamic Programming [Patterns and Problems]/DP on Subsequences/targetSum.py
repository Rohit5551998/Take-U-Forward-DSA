# mypy: disable-error-code="empty-body"
# QUESTION: Target Sum
# Assign '+' or '-' before each number in nums and count the number of
# expressions evaluating to target.
#
# Example 1:
# Input: nums = [1,1,1,1,1], target = 3
# Output: 5
#
# Constraints:
# 1 <= n <= 20
# 0 <= nums[i] <= 1000

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def find_target_sum_ways_brute(self, nums: List[int], target: int) -> int:
        pass

    def find_target_sum_ways_better(self, nums: List[int], target: int) -> int:
        pass

    def find_target_sum_ways_optimal(self, nums: List[int], target: int) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.find_target_sum_ways_optimal([1, 1, 1, 1, 1], 3)
