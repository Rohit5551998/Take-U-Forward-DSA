# mypy: disable-error-code="empty-body"
# QUESTION: Largest Divisible Subset
# Given distinct positive integers nums, return the largest subset such that
# every pair (a,b) satisfies a % b == 0 or b % a == 0.
#
# Example 1:
# Input: nums = [1,2,3]
# Output: [1,2]
#
# Constraints:
# 1 <= n <= 1000

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def largest_divisible_subset_brute(self, nums: List[int]) -> List[int]:
        pass

    def largest_divisible_subset_better(self, nums: List[int]) -> List[int]:
        pass

    def largest_divisible_subset_optimal(self, nums: List[int]) -> List[int]:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.largest_divisible_subset_optimal([1, 2, 3])
