# mypy: disable-error-code="empty-body"
# QUESTION: Ninja's Training
# A ninja trains for n days; each day he does one of 3 activities gaining
# points[i][j] merit, but cannot repeat the same activity on consecutive days.
# Maximize total merit points.
#
# Example 1:
# Input: points = [[1,2,5],[3,1,1],[3,3,3]]
# Output: 11
#
# Constraints:
# 1 <= n <= 10^5
# 0 <= points[i][j] <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def ninjas_training_brute(self, points: List[List[int]]) -> int:
        pass

    def ninjas_training_better(self, points: List[List[int]]) -> int:
        pass

    def ninjas_training_optimal(self, points: List[List[int]]) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.ninjas_training_optimal([[1, 2, 5], [3, 1, 1], [3, 3, 3]])
