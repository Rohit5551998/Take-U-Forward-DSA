# mypy: disable-error-code="empty-body"
# QUESTION: Minimum Path Sum
# Given an m x n grid of non-negative numbers, find a path from top-left to
# bottom-right minimizing the sum along the path (move only right or down).
#
# Example 1:
# Input: grid = [[1,3,1],[1,5,1],[4,2,1]]
# Output: 7
# Explanation: path 1->3->1->1->1.
#
# Constraints:
# 1 <= m, n <= 200
# 0 <= grid[i][j] <= 200

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def min_path_sum_brute(self, grid: List[List[int]]) -> int:
        pass

    def min_path_sum_better(self, grid: List[List[int]]) -> int:
        pass

    def min_path_sum_optimal(self, grid: List[List[int]]) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.min_path_sum_optimal([[1, 3, 1], [1, 5, 1], [4, 2, 1]])
