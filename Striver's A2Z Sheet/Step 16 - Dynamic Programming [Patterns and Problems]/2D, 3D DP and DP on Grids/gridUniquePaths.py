# mypy: disable-error-code="empty-body"
# QUESTION: Grid Unique Paths
# A robot starts at the top-left of an m x n grid and wants to reach the
# bottom-right, moving only right or down. Count the number of unique paths.
#
# Example 1:
# Input: m = 3, n = 2
# Output: 3
#
# Constraints:
# 1 <= m, n <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def unique_paths_brute(self, m: int, n: int) -> int:
        pass

    def unique_paths_better(self, m: int, n: int) -> int:
        pass

    def unique_paths_optimal(self, m: int, n: int) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.unique_paths_optimal(3, 2)
