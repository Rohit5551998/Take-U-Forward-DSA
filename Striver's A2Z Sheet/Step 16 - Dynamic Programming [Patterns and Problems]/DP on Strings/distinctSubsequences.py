# mypy: disable-error-code="empty-body"
# QUESTION: Distinct Subsequences
# Given strings s and t, return the number of distinct subsequences of s which
# equal t.
#
# Example 1:
# Input: s = "rabbbit", t = "rabbit"
# Output: 3
#
# Constraints:
# 1 <= len(s), len(t) <= 1000

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def num_distinct_brute(self, s: str, t: str) -> int:
        pass

    def num_distinct_better(self, s: str, t: str) -> int:
        pass

    def num_distinct_optimal(self, s: str, t: str) -> int:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.num_distinct_optimal("rabbbit", "rabbit")
