# mypy: disable-error-code="empty-body"
# QUESTION: Wildcard Matching
# Implement wildcard matching where '?' matches any single character and '*'
# matches any sequence (including empty). Return True if p matches all of s.
#
# Example 1:
# Input: s = "adceb", p = "*a*b"
# Output: True
#
# Constraints:
# 0 <= len(s), len(p) <= 2000

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def wildcard_match_brute(self, s: str, p: str) -> bool:
        pass

    def wildcard_match_better(self, s: str, p: str) -> bool:
        pass

    def wildcard_match_optimal(self, s: str, p: str) -> bool:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.wildcard_match_optimal("adceb", "*a*b")
