# mypy: disable-error-code="empty-body"
# QUESTION: User Input / Output
# Foundational/theory topic: how a program reads values from standard input and
# writes results to standard output. In Python this is input()/print(); the C++
# STL equivalents are cin/cout. Understanding buffered I/O and parsing tokens is
# the base skill for every competitive-programming and interview problem.
# Example 1:
# Input: 5 7
# Output: 12
# Explanation: Read two integers from stdin, add them, print the sum to stdout.
# Constraints:
# - Values are read as text and must be cast to the required type (int, float).
# - Output must match the expected format exactly (spacing, newlines).

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def user_input_output_brute(self) -> None:
        pass

    def user_input_output_better(self) -> None:
        pass

    def user_input_output_optimal(self) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.user_input_output_optimal(...)
