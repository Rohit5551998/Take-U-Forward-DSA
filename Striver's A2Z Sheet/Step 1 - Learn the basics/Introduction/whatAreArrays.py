# mypy: disable-error-code="empty-body"
# QUESTION: What are Arrays
# Foundational/theory topic: arrays store a fixed-order collection of elements in
# contiguous memory, giving O(1) index access. In Python the closest built-in is
# the list. This is the base data structure most algorithms build on.
# Example 1:
# Input: arr = [10, 20, 30], index = 1
# Output: 20
# Explanation: Indexing is O(1); arr[1] returns the second element.
# Constraints:
# - Indices are zero-based.
# - Elements are typically of a single logical type.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def what_are_arrays_brute(self, arr: List[int], index: int) -> None:
        pass

    def what_are_arrays_better(self, arr: List[int], index: int) -> None:
        pass

    def what_are_arrays_optimal(self, arr: List[int], index: int) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.what_are_arrays_optimal(...)
