# mypy: disable-error-code="empty-body"
# QUESTION: While Loops
# Foundational/theory topic: a while loop repeats a block as long as a condition
# stays true. It is used when the number of iterations is not known in advance,
# such as reading until end-of-input or converging on a value.
# Example 1:
# Input: n = 13
# Output: 4
# Explanation: Repeatedly divide by 2 counting steps until n becomes 0 -> 4 steps.
# Constraints:
# - The condition must eventually become false or the loop runs forever.
# - State updated inside the loop drives termination.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def while_loops_brute(self, n: int) -> None:
        pass

    def while_loops_better(self, n: int) -> None:
        pass

    def while_loops_optimal(self, n: int) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.while_loops_optimal(...)
