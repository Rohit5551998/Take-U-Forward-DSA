# mypy: disable-error-code="empty-body"
# QUESTION: For Loops
# Foundational/theory topic: a for loop repeats a block a known number of times,
# usually iterating over a range or a collection. It is the primary tool for
# traversing arrays and generating sequences.
# Example 1:
# Input: n = 5
# Output: 0 1 2 3 4
# Explanation: for i in range(n) yields each index from 0 to n-1.
# Constraints:
# - The loop variable is bounded by the range/collection length.
# - Off-by-one errors come from wrong start/stop bounds.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def for_loops_brute(self, n: int) -> None:
        pass

    def for_loops_better(self, n: int) -> None:
        pass

    def for_loops_optimal(self, n: int) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.for_loops_optimal(...)
