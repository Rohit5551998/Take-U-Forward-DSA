# mypy: disable-error-code="empty-body"
# QUESTION: If-Else Statements
# Foundational/theory topic: conditional branching. An if/elif/else chain evaluates
# boolean conditions in order and runs the first matching block, letting a program
# choose different paths based on data.
# Example 1:
# Input: n = -4
# Output: "Negative"
# Explanation: n < 0 is true, so the "Negative" branch runs.
# Constraints:
# - Conditions must be mutually exhaustive to cover every input.
# - Branch order matters when conditions overlap.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def if_else_statements_brute(self, n: int) -> None:
        pass

    def if_else_statements_better(self, n: int) -> None:
        pass

    def if_else_statements_optimal(self, n: int) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.if_else_statements_optimal(...)
