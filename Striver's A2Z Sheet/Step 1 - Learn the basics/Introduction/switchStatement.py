# mypy: disable-error-code="empty-body"
# QUESTION: Switch Statement
# Foundational/theory topic: multi-way branching on a single value. C++/Java use
# switch/case with break; Python expresses the same idea with match/case or a
# dictionary dispatch, mapping each discrete value to an action.
# Example 1:
# Input: day = 3
# Output: "Wednesday"
# Explanation: The case matching 3 maps to Wednesday.
# Constraints:
# - Cases compare against discrete constant values.
# - A default/else branch handles unmatched inputs.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def switch_statement_brute(self, day: int) -> None:
        pass

    def switch_statement_better(self, day: int) -> None:
        pass

    def switch_statement_optimal(self, day: int) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.switch_statement_optimal(...)
