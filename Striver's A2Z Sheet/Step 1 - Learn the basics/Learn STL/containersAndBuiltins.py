# mypy: disable-error-code="empty-body"
# QUESTION: Common Containers and Built-ins
# Foundational/theory topic: everyday Python containers (list, tuple, set, dict)
# and the built-in helpers that operate on them (len, sorted, sum, min, max, map,
# filter, zip, enumerate). Fluency with these removes boilerplate and speeds up
# problem solving.
# Example 1:
# Input: nums = [3, 1, 2]
# Output: [1, 2, 3]
# Explanation: sorted(nums) returns a new list in ascending order in O(n log n).
# Constraints:
# - sorted() returns a new list; list.sort() sorts in place.
# - Built-ins like sum/min/max run in O(n) over the iterable.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def containers_and_builtins_brute(self, nums: List[int]) -> None:
        pass

    def containers_and_builtins_better(self, nums: List[int]) -> None:
        pass

    def containers_and_builtins_optimal(self, nums: List[int]) -> None:
        pass


if __name__ == "__main__":
    sol = Solution()
    # sol.containers_and_builtins_optimal(...)
