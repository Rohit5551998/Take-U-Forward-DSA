# mypy: disable-error-code="empty-body"
# QUESTION: Reverse a stack using recursion
# Given a stack, reverse it using recursion only. No explicit loops and no extra
# stack/queue data structure are allowed; only the recursion call stack may be used.
# Example 1:
# Input: stack = [1, 2, 3, 4] (top is 4)
# Output: [4, 3, 2, 1] (top is 1)
# Explanation: The order of elements is fully reversed.
# Constraints:
# 1 <= stack size <= 100
# -100 <= element <= 100

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def reverse_stack_brute(self, stack: List[int]) -> List[int]:
        pass

    def reverse_stack_better(self, stack: List[int]) -> List[int]:
        pass

    def reverse_stack_optimal(self, stack: List[int]) -> List[int]:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.reverse_stack_optimal([1, 2, 3, 4]))
