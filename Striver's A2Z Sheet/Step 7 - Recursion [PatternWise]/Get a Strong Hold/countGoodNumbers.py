# mypy: disable-error-code="empty-body"
# QUESTION: Count Good numbers
# A digit string is "good" if the digits at even indices (0-indexed) are even
# and the digits at odd indices are prime (2, 3, 5, or 7).
# Given an integer n, return the total number of good digit strings of length n.
# Since the answer may be large, return it modulo 10^9 + 7.
# Example 1:
# Input: n = 1
# Output: 5
# Explanation: The good numbers of length 1 are "0", "2", "4", "6", "8".
# Example 2:
# Input: n = 4
# Output: 400
# Constraints:
# 1 <= n <= 10^15

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def count_good_numbers_brute(self, n: int) -> int:
        pass

    def count_good_numbers_better(self, n: int) -> int:
        pass

    def count_good_numbers_optimal(self, n: int) -> int:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.count_good_numbers_optimal(4))
