# mypy: disable-error-code="empty-body"
# QUESTION: Requirements Needed to Construct a Unique Binary Tree
# Given a pair of tree traversals, determine whether they are sufficient to uniquely
# construct the binary tree. Encode the traversal pair (preorder=1, inorder=2,
# postorder=3) and return whether the pair uniquely defines a tree.
#
# Example 1:
# Input: a = 1, b = 2  (preorder, inorder)
# Output: true
# Explanation: Preorder + inorder uniquely construct a binary tree.
#
# Constraints:
# a, b in {1, 2, 3} and a != b.
# 1 = preorder, 2 = inorder, 3 = postorder.


"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def requirementsForUniqueTraversals_brute(self, a: int, b: int) -> bool:
        pass

    def requirementsForUniqueTraversals_better(self, a: int, b: int) -> bool:
        pass

    def requirementsForUniqueTraversals_optimal(self, a: int, b: int) -> bool:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.requirementsForUniqueTraversals_optimal(1, 2))
