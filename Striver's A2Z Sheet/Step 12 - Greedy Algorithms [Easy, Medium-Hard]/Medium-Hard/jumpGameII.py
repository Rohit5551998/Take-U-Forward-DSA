# mypy: disable-error-code="empty-body"
# QUESTION: Jump Game II
# You are given a 0-indexed integer array nums of length n. You start at index 0.
# Each element nums[i] is the maximum jump length forward from index i. It is
# guaranteed you can reach the last index. Return the minimum number of jumps to
# reach index n - 1.
#
# Example 1:
# Input: nums = [2, 3, 1, 1, 4]
# Output: 2
# Explanation: Jump 1 step from index 0 to 1, then 3 steps to the last index.
#
# Constraints:
# 1 <= nums.length <= 10^4
# 0 <= nums[i] <= 1000
# It is guaranteed the last index is reachable.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def jump_game_ii_brute(self, nums: List[int]) -> int:
        pass

    def jump_game_ii_better(self, nums: List[int]) -> int:
        pass

    def jump_game_ii_optimal(self, nums: List[int]) -> int:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.jump_game_ii_optimal([2, 3, 1, 1, 4]))
