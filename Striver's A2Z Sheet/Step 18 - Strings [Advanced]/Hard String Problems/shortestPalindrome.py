# mypy: disable-error-code="empty-body"
# QUESTION: Shortest Palindrome
# You are given a string s. You can convert s to a palindrome by adding
# characters in front of it. Return the shortest palindrome you can find by
# performing this transformation.
# Example 1:
# Input: s = "aacecaaa"
# Output: "aaacecaaa"
# Explanation: The shortest palindrome is formed by prepending "a" to s.
# Example 2:
# Input: s = "abcd"
# Output: "dcbabcd"
# Explanation: Prepend "dcb" so the whole string reads the same forwards and
# backwards.
# Constraints:
# 0 <= s.length <= 5 * 10^4
# s consists of lowercase English letters only.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def shortest_palindrome_brute(self, s: str) -> str:
        pass

    def shortest_palindrome_better(self, s: str) -> str:
        pass

    def shortest_palindrome_optimal(self, s: str) -> str:
        pass


if __name__ == "__main__":
    sol = Solution()
    # print(sol.shortest_palindrome_optimal("aacecaaa"))
