# mypy: disable-error-code="empty-body"
# QUESTION: All Divisors of a Number
# Given an integer N, find and return all of its divisors in ascending order.
# Example 1:
# Input: N = 12
# Output: [1, 2, 3, 4, 6, 12]
# Explanation: These are all the numbers that divide 12 exactly.
# Example 2:
# Input: N = 7
# Output: [1, 7]
# Explanation: 7 is prime, so its only divisors are 1 and 7.
# Constraints:
# 1 <= N <= 10^9

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def allDivisors_brute(self, N: int) -> List[int]:
        pass

    def allDivisors_better(self, N: int) -> List[int]:
        pass

    def allDivisors_optimal(self, N: int) -> List[int]:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.allDivisors_optimal(12))
