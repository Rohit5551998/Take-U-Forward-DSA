# mypy: disable-error-code="empty-body"
# QUESTION: Find XOR of numbers from L to R
# Given two integers L and R, compute the XOR of all integers in the inclusive
# range [L, R], i.e. L ^ (L+1) ^ (L+2) ^ ... ^ R.
# Example 1:
# Input: L = 4, R = 7
# Output: 0
# Explanation: 4 ^ 5 ^ 6 ^ 7 = 0.
# Example 2:
# Input: L = 3, R = 9
# Output: 2
# Explanation: 3 ^ 4 ^ 5 ^ 6 ^ 7 ^ 8 ^ 9 = 2.
# Constraints:
# 1 <= L <= R <= 10^9

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def xorLtoR_brute(self, L: int, R: int) -> int:
        pass

    def xorLtoR_better(self, L: int, R: int) -> int:
        pass

    def xorLtoR_optimal(self, L: int, R: int) -> int:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.xorLtoR_optimal(4, 7))
