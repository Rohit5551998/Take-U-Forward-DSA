# mypy: disable-error-code="empty-body"
# QUESTION: Find the number that appears an odd number of times
# Given an array of integers where every element appears an even number of times
# except for exactly one element that appears an odd number of times, find and
# return that element.
# Example 1:
# Input: nums = [1, 2, 3, 2, 3, 1, 3]
# Output: 3
# Explanation: 1 and 2 each appear twice, while 3 appears three times (odd).
# Example 2:
# Input: nums = [5, 7, 5]
# Output: 7
# Explanation: 5 appears twice, 7 appears once (odd).
# Constraints:
# 1 <= len(nums) <= 10^5
# Exactly one element appears an odd number of times.

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""

from typing import List


class Solution:
    def oddOccurrence_brute(self, nums: List[int]) -> int:
        pass

    def oddOccurrence_better(self, nums: List[int]) -> int:
        pass

    def oddOccurrence_optimal(self, nums: List[int]) -> int:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.oddOccurrence_optimal([1, 2, 3, 2, 3, 1, 3]))
