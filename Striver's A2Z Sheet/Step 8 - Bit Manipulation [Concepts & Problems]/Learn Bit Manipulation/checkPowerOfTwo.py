# mypy: disable-error-code="empty-body"
# QUESTION: Check if a number is power of 2 or not
# Given an integer N, determine whether it is a power of two.
# A number is a power of two if it can be expressed as 2^k for some k >= 0.
# Example 1:
# Input: N = 16
# Output: True
# Explanation: 16 = 2^4, so it is a power of two.
# Example 2:
# Input: N = 18
# Output: False
# Explanation: 18 cannot be written as 2^k, so it is not a power of two.
# Constraints:
# 1 <= N <= 10^9

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def isPowerOfTwo_brute(self, N: int) -> bool:
        pass

    def isPowerOfTwo_better(self, N: int) -> bool:
        pass

    def isPowerOfTwo_optimal(self, N: int) -> bool:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.isPowerOfTwo_optimal(16))
