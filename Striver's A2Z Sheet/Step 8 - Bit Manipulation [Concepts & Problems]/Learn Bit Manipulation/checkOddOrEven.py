# mypy: disable-error-code="empty-body"
# QUESTION: Check if a number is odd or even
# Given an integer N, determine whether it is odd or even using bit manipulation
# (without using the modulo operator).
# Example 1:
# Input: N = 7
# Output: "odd"
# Explanation: 7 = 111 in binary; the least significant bit is 1, so it is odd.
# Example 2:
# Input: N = 10
# Output: "even"
# Explanation: 10 = 1010 in binary; the least significant bit is 0, so it is even.
# Constraints:
# 0 <= N <= 10^9

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def checkOddEven_brute(self, N: int) -> str:
        pass

    def checkOddEven_better(self, N: int) -> str:
        pass

    def checkOddEven_optimal(self, N: int) -> str:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.checkOddEven_optimal(7))
