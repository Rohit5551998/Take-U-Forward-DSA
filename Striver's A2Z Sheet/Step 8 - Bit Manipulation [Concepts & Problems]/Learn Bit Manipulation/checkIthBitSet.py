# mypy: disable-error-code="empty-body"
# QUESTION: Check if the i-th bit is set or not
# Given a number N and a bit position i (0-indexed from the right), determine
# whether the i-th bit of N is set (equal to 1) or not.
# Example 1:
# Input: N = 8, i = 3   (8 = 1000 in binary)
# Output: True
# Explanation: The 3rd bit of 1000 is 1, so it is set.
# Example 2:
# Input: N = 8, i = 2   (8 = 1000 in binary)
# Output: False
# Explanation: The 2nd bit of 1000 is 0, so it is not set.
# Constraints:
# 0 <= N <= 10^9
# 0 <= i <= 31

"""
#Brute Force:
1.
TC -> O(), SC -> O()

#Better Approach:
1.
TC -> O(), SC -> O()

#Optimal Approach:
1.
TC -> O(), SC -> O()

#KEY INSIGHT:
-
"""


class Solution:
    def checkIthBit_brute(self, N: int, i: int) -> bool:
        pass

    def checkIthBit_better(self, N: int, i: int) -> bool:
        pass

    def checkIthBit_optimal(self, N: int, i: int) -> bool:
        pass


# if __name__ == "__main__":
#     sol = Solution()
#     print(sol.checkIthBit_optimal(8, 3))
